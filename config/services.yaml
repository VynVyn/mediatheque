# This file is the entry point to configure your own services.
# Files in the packages/ subdirectory configure your dependencies.

# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices.html#use-parameters-for-application-configuration
parameters:

services:
    # default configuration for services in *this* file
    _defaults:
        autowire: true      # Automatically injects dependencies in your services.
        autoconfigure: true
        bind:
            $maintenance: false
            $path: '%kernel.project_dir%'

          # Toute les classe de symfony sont des service => referencement containner de service => injection de dependance dans les autre classe par le construct


         # Automatically registers your services as commands, event subscribers, etc.

    # makes classes in src/ available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    App\:
        resource: '../src/'
        exclude:
            - '../src/DependencyInjection/'
            - '../src/Entity/'
            - '../src/Kernel.php'


    App\EventSubscriber\DocumentReadSubscriber:
        arguments: 
            $stateFiles: 'counter.json'

    # App\Repository\CommentaireRepository:
    #     arguments:
    #         $registry: '@ManagerRegistry'
    # # @ => un service
    # App\Service\Tarification\Individuel:
    #     tags:
    #         - { name: 'app.tarification', key: 'tarification individuel' }

    # App\Service\Tarification\Two:
    #     tags:
    #         - { name: 'app.tarification', key: 'tarification_two' }

    App\Service\Tarification\Entreprise :
        tags:
            - { name: 'app.tarification', key: 'tarification_entreprise' }

    App\Service\Tarification\Tarification:
        arguments: 
            - !tagged_iterator { tag: app.tarification, default_priority_method: getPriority, index_by: 'key' }
        