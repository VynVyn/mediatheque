{% extends 'base.html.twig' %}

{% block title %}Artist{% endblock %}

{% block body %}
    <h1>Artist</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ artist.id }}</td>
            </tr>
            <tr>
                <th>FirstName</th>
                <td>{{ artist.firstName }}</td>
            </tr>
            <tr>
                <th>LasteName</th>
                <td>{{ artist.lasteName }}</td>
            </tr>
            <tr>
                <th>Birthday</th>
                <td>{{ artist.birthday ? artist.birthday|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            
            <tr>
                <th>DateOfDeath</th>
                <td>{{ artist.dateOfDeath ? artist.dateOfDeath|date('Y-m-d H:i:s') : '' }}</td>
            </tr>

        </tbody>
    </table>

    <a href="{{ path('artist_index') }}">back to list</a>

    <a href="{{ path('artist_edit', {'id': artist.id}) }}">edit</a>

    {{ include('admin/artist/_delete_form.html.twig') }}
{% endblock %}
