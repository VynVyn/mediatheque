<form method="post" action="{{ path('categorie_artist_delete', {'id': categorie_artist.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categorie_artist.id) }}">
    <button class="btn">Delete</button>
</form>
